import 'package:flutter/material.dart';
import '../../social/social_match_screen.dart';

/// Onglet Social → Social Match (spec §10), branché sur l'API réelle.
class SocialTab extends StatelessWidget {
  const SocialTab({super.key});

  @override
  Widget build(BuildContext context) => const SocialMatchScreen();
}
