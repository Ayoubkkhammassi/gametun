import 'package:flutter/material.dart';
import '../../squads/squads_screen.dart';

/// Onglet Squad (spec §8-9), branché sur l'API réelle.
class SquadTab extends StatelessWidget {
  const SquadTab({super.key});

  @override
  Widget build(BuildContext context) => const SquadsScreen();
}
